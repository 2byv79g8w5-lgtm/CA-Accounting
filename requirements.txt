fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1

# Only needed if DATABASE_URL points at Postgres instead of the default
# SQLite. Not installed by default because it has no pre-built wheel on
# newer Python versions (needs pg_config / a Postgres install to build from
# source) and local dev doesn't need it at all — install separately when
# you actually deploy against Postgres:
#   pip install psycopg2-binary==2.9.9
